import { FacturationNotId } from './facturation-not-id.model';

describe('FacturationNotId', () => {
  it('should create an instance', () => {
    expect(new FacturationNotId()).toBeTruthy();
  });
});
